def greeting(fn, text = "") :
    print(f"Hello{fn}")
    print(f"{text}")

def user({"name", "money","bag"})

is running = True
while is running :
    user_choose = input("""
        a) category([])
        b)show all product
        c)Admin mode
        d)quit
    """)
    if user_choose == a :
        show_
    



def store(label,price,desc) :
    return {
        "label" : label,
        "price" : price,
        "desc" : desc
    }







